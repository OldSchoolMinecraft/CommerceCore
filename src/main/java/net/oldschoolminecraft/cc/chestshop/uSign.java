package net.oldschoolminecraft.cc.chestshop;

import org.bukkit.block.Sign;

public class uSign
{
    public static boolean isValid(Sign sign)
    {
        return isValid(sign.getLines());
    }

    public static boolean isValid(String[] lines)
    {
        //TODO: check if the lines are valid
        return false;
    }

    private static void formatLines(String[] lines)
    {
        //
    }
}
